apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: nemo
spec:
  storageClassName: ebs-auto-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi